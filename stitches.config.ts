import { createStitches } from '@stitches/react';

export const { styled, css } = createStitches({
  theme: {
    colors: {
      white: '#fff',
      gray100: '#ccc',
      gray300: '#aaa',
      black: '#111',
      red: 'crimson',
      green: 'green',
    },
  },
});
